---
id: "api:cppenum:ERefereeClientState"
title: "ERefereeClientState"
source: "https://developer.gp.qq.com/api/cppenum/detail/ERefereeClientState.json"
category: "API Wiki/cppenum"
kind: "cppenum"
api_root: "https://developer.gp.qq.com/api/"
---

# ERefereeClientState

## Variables

| Name | Type/Value | Description |
|---|---|---|
| `None` | `0` | - |
| `IsInFreeViewMode` | `-1` | - |
| `IsInForceTPPMode` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `IsInTPPFreeFollowMode` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `IsEnableVehicleMode` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `OpenningEntireMap` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
