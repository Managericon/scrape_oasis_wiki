---
id: "api:cppenum:EPayloadOperation"
title: "EPayloadOperation"
source: "https://developer.gp.qq.com/api/cppenum/detail/EPayloadOperation.json"
category: "API Wiki/cppenum"
kind: "cppenum"
api_root: "https://developer.gp.qq.com/api/"
---

# EPayloadOperation

## Variables

| Name | Type/Value | Description |
|---|---|---|
| `Add` | `-1` | - |
| `Remove` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `Modify` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `Empty` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
