---
id: "api:cppenum:ERainbowSwingBuildType"
title: "ERainbowSwingBuildType"
source: "https://developer.gp.qq.com/api/cppenum/detail/ERainbowSwingBuildType.json"
category: "API Wiki/cppenum"
kind: "cppenum"
api_root: "https://developer.gp.qq.com/api/"
---

# ERainbowSwingBuildType

## Variables

| Name | Type/Value | Description |
|---|---|---|
| `None` | `0` | - |
| `Left` | `-1` | - |
| `LeftMiddle` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `RightMiddle` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `Right` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `LeftAndRight` | `9` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
