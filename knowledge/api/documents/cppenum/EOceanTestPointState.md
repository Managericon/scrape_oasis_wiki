---
id: "api:cppenum:EOceanTestPointState"
title: "EOceanTestPointState"
source: "https://developer.gp.qq.com/api/cppenum/detail/EOceanTestPointState.json"
category: "API Wiki/cppenum"
kind: "cppenum"
api_root: "https://developer.gp.qq.com/api/"
---

# EOceanTestPointState

## Variables

| Name | Type/Value | Description |
|---|---|---|
| `EOceanTestPoint_UnTested` | `-1` | - |
| `EOceanTestPoint_NoAssociation` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `EOceanTestPoint_AboveOcean` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `EOceanTestPoint_TouchingOcean` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
