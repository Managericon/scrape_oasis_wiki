---
id: "api:cppenum:ERetargetAvatarAdaptSpace"
title: "ERetargetAvatarAdaptSpace"
source: "https://developer.gp.qq.com/api/cppenum/detail/ERetargetAvatarAdaptSpace.json"
category: "API Wiki/cppenum"
kind: "cppenum"
api_root: "https://developer.gp.qq.com/api/"
---

# ERetargetAvatarAdaptSpace

## Variables

| Name | Type/Value | Description |
|---|---|---|
| `RAAS_None` | `0` | - |
| `RAAS_MasterComponent_RootBoneSpace` | `-1` | - |
| `RAAS_MasterComponent_ParentSpace` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `RAAS_RetargetComponent_RootBoneSpace` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `RAAS_RetargetComponent_ParentSpace` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `RAAS_MasterComponent_MeshShift` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `RAAS_RetargetComponent_ParentSpace_CopyPose` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
