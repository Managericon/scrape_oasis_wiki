---
id: "api:cppenum:EBUIProperty"
title: "EBUIProperty"
source: "https://developer.gp.qq.com/api/cppenum/detail/EBUIProperty.json"
category: "API Wiki/cppenum"
kind: "cppenum"
api_root: "https://developer.gp.qq.com/api/"
---

# EBUIProperty

## Variables

| Name | Type/Value | Description |
|---|---|---|
| `UIP_None` | `0` | - |
| `UIP_DestroyOnHide` | `-1` | - |
| `UIP_AsyncCreation` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `UIP_GCOnDestroy` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `UIP_LuaFullGCOnDestroy` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `UIP_Default` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
