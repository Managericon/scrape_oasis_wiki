---
id: "api:cppenum:EDamageEventFlags"
title: "EDamageEventFlags"
source: "https://developer.gp.qq.com/api/cppenum/detail/EDamageEventFlags.json"
category: "API Wiki/cppenum"
kind: "cppenum"
api_root: "https://developer.gp.qq.com/api/"
---

# EDamageEventFlags

## Variables

| Name | Type/Value | Description |
|---|---|---|
| `None` | `0` | - |
| `Headshot` | `-1` | - |
| `FallDown` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `Dead` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `KillAll` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
| `DirectlyDead` | `-1` | -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1.  -- the value appears as -1 more due to UHT parse error rather than it actually be -1. |
